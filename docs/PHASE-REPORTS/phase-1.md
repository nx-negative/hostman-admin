# Phase 1 — Admin Auth (code + password + TOTP + multi-admin)
scope: admin_users + sessions + recovery_codes + audit_log; argon2id + TOTP + EdDSA JWT session; mother_admin-only admin management; login wizard UI.
built: backend `common/crypto.rs` (AES-256-GCM field enc, Crockford base32, argon2, Ed25519 root key + JWT EdDSA, TOTP, base32) · `common/ratelimit.rs` (sliding-window limiter + 5/15 lockout) · `domain` (admin_user/admin_session/recovery_code/audit_log entities + migration with PG enums) · `admin-api` (state, auth login/totp-verify/refresh/logout/me, admins list/create/delete, AppJson extractor, AuthAdmin extractor, general rate-limit middleware, setup bootstrap) · frontend `lib/auth.ts` · `app/auth.tsx` (AuthProvider) · `features/auth/login-wizard.tsx` (credentials→QR/TOTP→recovery) · `features/auth/admins-page.tsx` (mother-only mgmt) · shell logout + user badge + conditional Admins nav · login route.
checks: fmt ✓ clippy ✓ cargo test 15✓ (admin-api 4 + common 11) · tsc ✓ vitest 8✓ build ✓ · `bun run dev` smoke ✓ (UI 200, API proxy 200, /auth/me 401 no session).
human test: 1) `cargo run -p admin-api` + `bun run dev`  2) http://localhost:5173 → /login → enter mother code+password → QR → TOTP → recovery codes → Dashboard  3) first login QR; wrong TOTP rejected; tab close → re-login; mother sees Admins nav + can add/remove; admin doesn't; logout works.
known limitations: CORS permissive dev-only (P8 hardens); rate limits + lockout in-memory single-box (fine, spec = 1 server); JWT needs `aws_lc_rs` feature (jsonwebtoken 11 requirement).
blockers/questions for owner: none.
